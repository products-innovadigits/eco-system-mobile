name: strategy_package
description: Nawah Strategy module.
version: 1.0.0

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  core_package:
    path: ../core_package

  fl_chart: ^1.0.0
